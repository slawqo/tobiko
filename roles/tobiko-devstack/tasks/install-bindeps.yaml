---

- name: "ensure DevStack bindeps are installed"
  become: true
  package:
    name:
      - git
      - iptables
      - python3
      - python3-pip
      - python3-systemd
      - rsync
      - sudo
